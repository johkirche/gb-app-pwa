// The version shown under „Über die App", and in the summary the settings
// overview puts under that row. Hardcoded for now — a real setup would hand it
// down from package.json at build time.
export const APP_VERSION = '1.0.0';
